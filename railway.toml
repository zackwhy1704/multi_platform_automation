[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "uvicorn gateway.app:app --host 0.0.0.0 --port 8000 --workers 2"
healthcheckPath = "/webhook"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
