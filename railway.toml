[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/webhook?hub.mode=subscribe&hub.verify_token=healthcheck"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
